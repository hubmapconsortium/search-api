mappings:
  properties:
    "new_unexpected_field": {
      "type": "text",
      "copy_to": "all_text" 
    }
    "all_text": {
      "type": "text"
    }
  # dynamic_templates:
  #   - 
  #     copy_every_string: # This is an arbitrary label.
  #       match_mapping_type: string
  #       mapping:
  #         copy_to: all_strings
  #         type: text
